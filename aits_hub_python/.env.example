# 应用配置
APP_NAME=AITS 智能测试管理平台
APP_ENV=development
DEBUG=true
SECRET_KEY=change-this-to-a-random-secret-key-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# LLM API Key 独立加密密钥（生产强烈建议配置：32 字节随机数经 urlsafe base64 编码）。
# 未配置时从 SECRET_KEY 哈希派生（向后兼容）。生成示例：
#   python -c "import base64;print(base64.urlsafe_b64encode(__import__('os').urandom(32)).decode())"
# FERNET_KEY=

# 数据库配置（MySQL）
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=aits_platform

# Redis 配置（用于缓存和 Celery 任务队列）
REDIS_URL=redis://localhost:6379/0

# Celery 配置（可选，使用默认值即可）
# CELERY_BROKER_URL=redis://localhost:6379/0
# CELERY_RESULT_BACKEND=redis://localhost:6379/0
# CELERY_WORKER_CONCURRENCY=4

# CORS
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# 默认 LLM 配置（可在后台模型管理中修改）
DEFAULT_LLM_PROVIDER=openai_compatible
DEFAULT_LLM_BASE_URL=https://api.deepseek.com/v1
DEFAULT_LLM_API_KEY=sk-xxxxxx
DEFAULT_LLM_MODEL=deepseek-chat
